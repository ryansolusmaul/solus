# solus

Animasi pendulum sederhana berbasis HTML5 Canvas.

## Menjalankan

Buka `index.html` langsung di browser, atau jalankan server lokal:

```bash
python3 -m http.server 8000
```

Lalu akses `http://localhost:8000`.
